package world_test

import (
	"testing"
)

func TestNewWorld(t *testing.T) {
	t.Run("cells get created correctly", func(tt *testing.T) {
		// want := []*world.Cell{
		// 	&world.Cell{

		// 	},
		// }
		// w := world.NewWorld(10, 10, world.EdgeModeLoop, world.StartModeFixed)
		// assert.DeepEqual(w.Cells, want)
	})
}

func TestFindCell(t *testing.T) {

}
