version: '3'

services:
  evo-server:
    # ports: 
    #     - "8080:8080"
    entrypoint: ""
  evo-web:
    ports:
        - "9090:80"
    volumes: 
        - ./out/static:/usr/share/nginx/html
  watcher:
    image: relnod/evo-dev:latest
    volumes: 
        - ./:/go/src/github.com/relnod/evo/
    ports: 
        - "8080:8080"
